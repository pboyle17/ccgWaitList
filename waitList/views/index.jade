extends layout

block content
  h1= title
  h2 Welcome to #{title}
  hr
  each player in players
    p= player.name
    p= player.game
    button(type='button' class='delete'  id='#{player.id}') Delete #{player.name}
    hr

  ul Add new Player:
    form(action='/', method="post")
      input(id='name' autofocus type='text', name='name',  placeholder='Name')
      select(name='game')
        option 1/3 NLHE
        option 1/2/5 PLO
        option 1/1 NLHE
        option 2/5 NLHE
        input(id='time' type='hidden' name='time')
      input(id='add' type='submit' value='add')

  button(id='table') Create a New Table

  #message

  a(href='/tables') Go to the tables view
